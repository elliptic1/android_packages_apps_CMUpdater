TODO: add this back once we have a way of doing backups that is not recovery specific

--- Add a new header to preference_headers.xml ---
--- Add this to xml/pref_backup.xml ---
<CheckBoxPreference
    android:key="pref_backup_rom"
    android:defaultValue="true"
    android:title="@string/backup_rom_title" 
    android:summary="@string/backup_rom_summary" />

--- Add this to updater/preferences/BackupFragment.java-----
    // See if backups are enabled and add the nandroid flag
    mBackupRom = (CheckBoxPreference)
    prefSet.findPreference(Constants.BACKUP_PREF);
    mBackupRom.setChecked(mPrefs.getBoolean(Constants.BACKUP_PREF, true));

         
